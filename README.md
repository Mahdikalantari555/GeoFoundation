# GeoFoundation

Local-first, offline-by-default AI platform for remote sensing research.
Monorepo assembling the GeoMemory knowledge engine, the GeoAgent SDK, and
the gateway server + web apps built on top.

> Charter, architecture, and specs live in `docs/`. Status: scaffolding.
